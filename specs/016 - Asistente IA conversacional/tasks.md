# Tasks 016
 
- [X] T1 — Modelos SQLAlchemy (ConversacionIA, MensajeIA, BorradorIA) — hecho: añadido `datos_cuestionario` (JSONB), `motivo_rechazo` (Text, nullable), renombrado `TIPOS_BORRADOR: nutricion → plan_nutricional`
- [X] T2 — Migración Alembic — hecho: `5fe144b91322_borradores_ia_cuestionario_motivo.py`, `down_revision = "2abd213f134d"` confirmado con `alembic heads`, y `alembic upgrade head` ya ejecutado (confirmado en terminal: `Running upgrade 2abd213f134d -> 5fe144b91322`)
- [X] T3 — Schemas Pydantic — hecho, actualizado 2026-08-24: `WizardBorradorRequest` (con validaciones de rango y campos obligatorios condicionados por `tipo_borrador`), `BorradorOut` con `datos_cuestionario` y `motivo_rechazo`, `BorradorEditar` (PATCH) y `BorradorRechazar` (motivo obligatorio). Añadido `sexo` (`masculino`\|`femenino`\|`prefiero_no_decirlo`, obligatorio) tras corrección de spec: se había puesto por error en el perfil del socio (que no lo tiene), se mueve al cuestionario porque sí influye en la calidad del borrador. `service.py` ya lo incluye en el prompt sin cambios (arma el prompt genéricamente a partir del dict)
- [X] T4 — Endpoint chat (POST /asistente/mensaje) con prompt de sistema restringido — sin cambios
- [X] T5 — Endpoint generación de borrador con datos del socio — hecho: `router.py` acepta `WizardBorradorRequest` y guarda `datos_cuestionario`; `service.generar_contenido_borrador()` ahora recibe el dict completo del cuestionario y arma el prompt con todos los campos (no solo un objetivo suelto); `FORMATOS_BORRADOR["nutricion"]` renombrado a `"plan_nutricional"`
- [X] T6 — Endpoint listado de borradores pendientes (entrenador) — hecho: no requería cambio de código, `BorradorOut` (T3) ya expone `datos_cuestionario` y `motivo_rechazo` en esta misma respuesta
- [X] T7 — Endpoint aprobar: llama a `crear_rutina_desde_borrador()` / `crear_plan_nutricional_desde_borrador()` (módulo 007) para crear el registro real con `origen=ia` y `borrador_id`; si el mapeo del JSON del borrador falla, rechaza la aprobación con error explícito — sin cambios (usa `contenido`, ya editado si aplica T11)
- [X] T8 — Endpoint rechazar — hecho: exige `BorradorRechazar.motivo` en el body, se persiste en `motivo_rechazo`
- [X] T9 — Manejo de fallback si Ollama no responde — sin cambios
- [X] T10 — Tests de integración — hecho: `test_asistente_ia.py` actualizado al contrato de `WizardBorradorRequest` (payloads completos válidos, 422 por campos condicionados por tipo, validación de rangos, persistencia/exposición de `datos_cuestionario`, PATCH solo pendiente/socio-asignado con 403/409, rechazar exige motivo, aprobar sigue funcionando). 35/35 en `test_asistente_ia.py`; suite completa 257/259 (2 fallos preexistentes no relacionados: `test_informes.py::test_informe_ocupacion`, `test_notificaciones.py::test_procesar_cola_marca_enviada_si_exitoso`). Bug encontrado y corregido: los helpers `_crear_borrador` no seteaban `datos_cuestionario` (NOT NULL desde la migración) y corrompían el rollback de test, filtrando filas a la BD real — limpiado en Postgres. Actualizado 2026-08-24: se añadió `sexo` a `RUTINA_PAYLOAD` y `PLAN_NUTRICIONAL_PAYLOAD` tras la corrección de spec (sexo pasó a obligatorio en el Paso 2) — vuelve a estar 35/35 en verde
- [X] T11 — Endpoint `PATCH /asistente/borradores/{id}` (entrenador) — hecho: edita `contenido` antes de aprobar, sin cambiar `estado`. Habilita el flujo "editar y aprobar" de la spec
### Frontend (Fase 5 del plan) — MODIFICACIÓN de páginas ya existentes y en producción, no creación desde cero (ver CHANGELOG.md 2026-08-23)
 
- [X] T12-URGENTE — Arreglar la rotura de `AsistentePage.jsx` y `BorradoresPendientesPage.jsx` por los cambios de backend — hecho: formulario plano temporal en `AsistentePage.jsx` cubriendo todos los campos de `WizardBorradorRequest` (sustituido después por T13); textarea de `motivo` obligatoria antes de rechazar en `BorradoresPendientesPage.jsx`; `TIPO_LABEL` renombrado en ambas páginas. Verificado en vivo contra el backend real: `POST /asistente/borrador` → 201 (rutina y plan_nutricional), `POST .../rechazar` → 200 con motivo. `npm run build`/`lint` limpios
- [X] T13 — Componente `WizardBorrador` (contenedor) — hecho: `WizardBorrador.jsx` en `portal-socio/components/WizardBorrador/`, estado único `respuestas` que sobrevive la navegación, registro `PASOS` (6 entradas: id + título + componente) para que T14-T16 añadan cada paso sin tocar el contenedor, barra de progreso, botones Siguiente/Atrás (Atrás deshabilitado en paso 0, nunca borra datos), botón final deshabilitado hasta que T16 conecte `onCompletar` con el envío real. Integrado en `AsistentePage.jsx`, sustituyendo el formulario plano de T12. Verificado con Playwright: navegación completa de los 6 pasos adelante y atrás sin perder estado (6/6 checks). `npm run build`/`lint` limpios
- [X] T14 — Paso 0 (tipo) y Paso 1 (confirmación de `edad` y `tipo_membresia` — YA NO `sexo`/`nombre`, ver corrección de spec 2026-08-24; si falta `fecha_nacimiento` en el perfil, permite editarla y la persiste vía perfil de Socio 003) — hecho: `PasoTipo.jsx` (selección `tipo_borrador`) y `PasoPerfil.jsx` (edad derivada de `Socio.fecha_nacimiento` en cliente — no hay endpoint que la exponga ya calculada —, y `tipo_membresia` vía `membresiasService.listarMisMembresias()` + `planesService.listarPlanesPublico()`, mismo patrón que ya usaba `PerfilPage.jsx`), registrados en `PASOS` de `WizardBorrador.jsx` sin tocar su contrato. `edad`/`tipo_membresia` no se escriben en el estado `respuestas` del wizard porque la spec corregida aclara que no forman parte de `datos_cuestionario`. Bug de backend encontrado y corregido: `fecha_nacimiento` no estaba en `CAMPOS_EDITABLES_PROPIO_SOCIO` (`socios/schemas.py`), así que el caso borde de la spec (perfil sin fecha de nacimiento, editable y guardado vía el PUT ya existente de 003) devolvía 403 — añadido a ese set, sigue siendo el mismo endpoint, no uno nuevo. Verificado con Playwright (15/15 checks): navegación 0↔1 sin perder selección; socio con perfil completo muestra edad/membresía en solo lectura sin nombre/sexo; socio sin `fecha_nacimiento` (simulado interceptando la respuesta de `GET /socios/me`, ya que la columna es `NOT NULL` en BD y no se puede forzar el caso editando directamente la fila) muestra el campo editable y, al guardar, el `PUT /socios/{id}` real persiste el dato — confirmado con una consulta a la BD después (`1998-04-20`). `npm run build`/`lint` limpios
- [X] T15 — Paso 2 (datos físicos, incluye `sexo` — obligatorio para ambos tipos, ver spec corregida 2026-08-24) y Paso 3 (preferencias), campos condicionados por `tipo_borrador` — hecho: `PasoDatosFisicos.jsx` (`sexo`, `peso_kg`, `altura_cm` para ambos tipos; `nivel_actividad_actual` solo rutina, `restricciones_medicas` solo plan_nutricional) y `PasoPreferencias.jsx` (`dias_disponibles_semana`/`equipamiento`/`lesiones_zonas_evitar` solo rutina; `preferencia_alimentaria`/`comidas_al_dia`/`alimentos_excluir` solo plan_nutricional), registrados en `PASOS` de `WizardBorrador.jsx` sin tocar su contrato ni su contenedor. Sin validación de rango custom todavía (solo atributos nativos `min`/`max`/`type="number"`) — eso es T17. Verificado con Playwright (17/17 checks): con `tipo_borrador="rutina"` el paso 3 muestra los campos de rutina y no los de nutrición y viceversa; navegación 2↔3 sin perder lo introducido (sexo, peso, equipamiento); `sexo` obligatorio y visible en ambos tipos. `npm run build`/`lint` limpios (mismos 3 warnings preexistentes no relacionados, sin warnings nuevos)
- [X] T16 — Paso 4 (objetivo) y Paso 5 (resumen y envío) — llama a `POST /asistente/borrador` con el payload completo del wizard — hecho: `PasoObjetivo.jsx` (`objetivo_principal` chips, `objetivo_detalle` textarea `maxLength=280` sin validación estricta, eso es T17) y `PasoResumen.jsx` (solo lectura de todo lo introducido en pasos 1-4, incluyendo `edad`/`tipo_membresia` recalculados igual que en `PasoPerfil.jsx` ya que ese estado no se comparte por diseño de T14; "volver a corregir" se resuelve con el botón "Atrás" ya existente del contenedor, sin añadir navegación por salto — no se tocó `WizardBorrador.jsx` salvo registrar los dos componentes en `PASOS` y borrar el `PasoPlaceholder` ya sin uso). El envío real vive en `AsistentePage.jsx::handleCompletarWizard`, pasado como `onCompletar` al wizard sin cambiar su contrato: reutiliza `asistenteService.solicitarBorrador` (la función ya adaptada en T12, no se creó ninguna llamada nueva). Éxito (201): sustituye el wizard por el aviso obligatorio de la spec ("Esto es un borrador generado por IA, pendiente de revisión profesional"). Error 503: reutiliza `mensajeErrorApi` (ya soportaba un `mensaje503` específico por caso de uso desde el fix del 2026-08-23) con un texto propio de este flujo, no el genérico del chat; el wizard permanece montado con `respuestas` intacto, así que no se pierden datos. Verificado con Playwright contra backend + Ollama reales: rutina y plan_nutricional generan `POST /asistente/borrador` → 201 (la rutina falló una vez por JSON inválido del modelo — comportamiento 503 esperado y ya cubierto, no relacionado con este código — y tuvo éxito al reintentar); el entrenador asignado (`smoke-entrenador@test.com`, asignación creada en BD para la prueba) ve ambos borradores en `/admin/borradores-ia` con el aviso de pendiente de revisión; simulación de 503 interceptando la red confirma el mensaje específico, wizard no roto, y el resumen sigue mostrando los datos introducidos. `npm run build`/`lint` limpios (mismos 3 warnings preexistentes no relacionados, sin warnings nuevos)
- [X] T17 — Validación cliente de rangos, replica las validaciones del backend (T3): sexo, peso_kg, altura_cm, dias_disponibles_semana, comidas_al_dia, objetivo_detalle — hecho: toda la lógica vive en `WizardBorrador.jsx` (función `validarPaso(pasoId, respuestas)`, replica exactamente `WizardBorradorRequest` de `schemas.py`), sin cambiar el contrato de ningún `Paso*.jsx` (cada uno sigue recibiendo solo `titulo`/`respuestas`/`actualizarRespuestas`) — el contenedor ya tenía acceso a `respuestas` completo, así que valida ahí mismo y muestra el mensaje inline sobre los botones, además de `disabled` en "Siguiente" (ambos, no solo el atributo). Reglas: paso tipo (`tipo_borrador` obligatorio), paso perfil (`fecha_nacimiento_ok` obligatorio — ver bug/hallazgo abajo), paso datos físicos (`sexo` obligatorio; `peso_kg` 20-300; `altura_cm` 100-250; `nivel_actividad_actual` obligatorio solo si rutina), paso preferencias (rutina: `dias_disponibles_semana` 1-7 y `equipamiento` obligatorios; plan_nutricional: `preferencia_alimentaria` obligatorio y `comidas_al_dia` 2-6), paso objetivo (`objetivo_principal` obligatorio; `objetivo_detalle` no bloquea vacío, solo si por algún medio superase 280 caracteres pese al `maxLength` ya existente de T16). Campos opcionales (`restricciones_medicas`, `lesiones_zonas_evitar`, `alimentos_excluir`) sin validación, según spec. Hallazgo: el Paso 1 (`PasoPerfil.jsx`) no tenía ninguna validación y ni siquiera compartía el estado de `fecha_nacimiento` con el wizard (solo usaba `titulo`, ignoraba `respuestas`/`actualizarRespuestas` aunque el contenedor ya se los pasaba) — se añadió un `useEffect` que sincroniza `fecha_nacimiento_ok` al estado compartido cada vez que cambia localmente (al cargar el perfil o al guardar la fecha vía el formulario ya existente de T14), y `actualizarRespuestas` se memoizó con `useCallback` en el contenedor para que ese efecto no entre en bucle ni dispare un warning de `exhaustive-deps` nuevo. Verificado con Playwright (28/28 checks): cada paso bloquea con mensaje inline al dejar un campo obligatorio vacío y desbloquea al corregirlo; peso_kg/altura_cm/dias_disponibles_semana/comidas_al_dia bloquean fuera de rango y aceptan los límites exactos (20/300, 100/250, 1/7, 2/6); paso perfil bloqueado cuando `GET /socios/me` no trae `fecha_nacimiento` (interceptado) y se desbloquea tras guardarla con el formulario ya existente; wizard completo con datos válidos para rutina y plan_nutricional siguen generando `POST /asistente/borrador` → 201 sin regresión sobre T16 (aviso obligatorio visible). `npm run build`/`lint` limpios (mismos 3 warnings preexistentes no relacionados, sin warnings nuevos)
- [X] T18 — Modificar `BorradoresPendientesPage.jsx` (existente): añadir panel de `datos_cuestionario` (solo lectura) junto al panel del borrador (que pasa a ser editable), usa `GET /asistente/borradores-pendientes` (T6, ya expone `datos_cuestionario`). Nota: `edad`/`tipo_membresia` NO están en `datos_cuestionario`, hay que obtenerlos del perfil del socio aparte (ver spec, sección "Vista del entrenador") — hecho: panel izquierdo `DatosCuestionario` pinta con etiquetas legibles todos los campos de `datos_cuestionario` según `tipo` (comunes: sexo/peso/altura/objetivo; condicionales: nivel_actividad/dias_disponibles/equipamiento/lesiones para rutina, restricciones_medicas/preferencia_alimentaria/comidas_al_dia/alimentos_excluir para plan_nutricional). `edad`/`tipo_membresia` no se añadió ningún endpoint nuevo: se reutiliza `sociosService.obtenerFichaSocio(socioId)` (ya usado por `FichaSocioPage.jsx` de specs/017) para `fecha_nacimiento` (edad calculada igual que `PasoPerfil.jsx`/`service.py::calcular_edad`) y `membresia.plan_id`, cruzado con `membresiasService.listarPlanes()` para el nombre del plan — mismo patrón de cruce que ya usaba esta página con `sociosService.listarSocios()` para el email (CHANGELOG 2026-08-23). Panel derecho: `ContenidoBorrador` (texto plano) sustituido por `EditorBorrador`, un `<textarea>` con estado local propio por borrador (`borradoresEditados`, keyed por `borrador.id`, inicializado con `JSON.stringify(contenido, null, 2)`); el guardado a `PATCH /asistente/borradores/{id}` queda para T19, no se tocó. Acciones Aprobar/Rechazar y layout de las cards sin cambios. Verificado con Playwright (18/18 checks) logueado como entrenador con 6 borradores pendientes reales (incluye 1 rutina y 1 plan_nutricional nuevos de esta verificación): panel izquierdo con las etiquetas y valores correctos según tipo, edad/membresía resueltos sin "—", y el textarea del borrador acepta texto editado por el entrenador. `npm run build`/`lint` limpios (mismos 3 warnings preexistentes, ninguno nuevo)
- [X] T19 — Acciones de revisión en `BorradoresPendientesPage.jsx`: aprobar tal cual (T7, ya funciona), añadir "editar y aprobar" (llama a `PATCH .../{id}` de T11 y luego aprobar) — el textarea de `motivo` ya se añadió en T12 — hecho: nuevo botón "Guardar cambios y aprobar" junto al "Aprobar" existente (sin tocarlo). `handleGuardarYAprobar` hace `JSON.parse` del texto de `borradoresEditados[borrador.id]` (estado de T18); si falla, muestra error inline (`erroresEdicion`, por borrador) y no llama a ningún endpoint. Si el parseo es válido, llama a `asistenteService.editarBorrador(id, contenido)` (nueva función añadida al servicio ya existente de este módulo, `PATCH /asistente/borradores/{id}` con `{ contenido }`, backend ya expuesto en T11); si el PATCH falla (403/404/409), muestra el error inline y no continúa. Si el PATCH tiene éxito, reutiliza `handleAprobar` (la misma función que ya usa el botón "Aprobar", sin duplicar lógica) para el `POST .../aprobar`. Verificado con Playwright contra backend real: (1) JSON inválido en el textarea bloquea sin llamar a `PATCH` y muestra el error (2/2 checks); (2) JSON válido editado (nombre de la rutina cambiado) → `PATCH` 200 seguido de `aprobar` 200, la card desaparece de pendientes, y la rutina real creada en `GET /rutinas/{socio_id}` tiene el nombre EDITADO, no el original — prueba funcional de que el orden PATCH→aprobar se respeta (si aprobar hubiera leído el contenido antes del PATCH, el nombre creado sería el original) (6/8 checks, ver nota); (3) "Aprobar" normal sin tocar el textarea sigue funcionando sin regresión: no llama a `PATCH`, aprueba con 200, la card desaparece, y el registro creado conserva el nombre ORIGINAL (4/4 checks). Nota: durante la verificación se detectó un bug preexistente no relacionado con T19 — el validador `_validar_contenido_rutina` (`entrenamiento/service.py`, `DIAS_SEMANA` sin tildes) rechaza con 400 el contenido de algunas rutinas generadas por Ollama que usan `"miércoles"` con tilde; afecta igual al botón "Aprobar" ya existente y al nuevo "Guardar cambios y aprobar" (mismo endpoint de aprobar), confirmado reproduciéndolo con ambos botones — no es una regresión de T19, es un problema de generación de contenido/validación en el módulo 007, fuera del alcance de esta task (frontend de panel-admin). `npm run build`/`lint` limpios (mismos 3 warnings preexistentes, ninguno nuevo)
- [X] T20 — Ampliar los servicios de API existentes de portal-socio y panel-admin (no crear un `asistenteApi.js` centralizado nuevo, seguir la convención ya usada en el proyecto) con las llamadas nuevas: wizard completo, PATCH de edición — auditoría de los 7 endpoints de la Fase 1 de plan.md contra `frontend/src/portal-socio/services/asistenteService.js` y `frontend/src/panel-admin/services/asistenteService.js`: los 7 ya estaban cubiertos por funciones de servicio existentes (`POST /asistente/mensaje` → `enviarMensaje`, `GET /asistente/historial` → `obtenerHistorial`, `POST /asistente/borrador` → `solicitarBorrador` en portal-socio, ya adaptada en T12/T16 al payload completo del wizard; `GET /asistente/borradores-pendientes` → `listarBorradoresPendientes`, `PATCH /asistente/borradores/{id}` → `editarBorrador` añadida en T19, `POST /asistente/borradores/{id}/aprobar` → `aprobarBorrador`, `POST /asistente/borradores/{id}/rechazar` → `rechazarBorrador`, todas en panel-admin). Se comprobó además que `AsistentePage.jsx` y `BorradoresPendientesPage.jsx` no tienen ninguna llamada directa a `fetch`/`axios`/`apiClient` (grep sin resultados en ambos), solo invocan `asistenteService.*`. No se creó ningún servicio nuevo ni se movió código, porque no había nada que mover: no hizo falta cambiar código, se deja constancia de la auditoría. `npm run build`/`lint` no re-ejecutados por no haber cambios de código.
- [X] T21 — Tests/verificación de frontend: wizard (validaciones y navegación) + las tres acciones de revisión del entrenador, siguiendo el mismo criterio de verificación manual end-to-end ya usado en el resto del changelog del proyecto — verificación final de todo T13-T20 en una sola pasada con Playwright contra backend + Ollama reales (51/51 checks), usando un socio de prueba nuevo (`ia-t21-socio-*@test.com`, dado de alta y asignado a `smoke-entrenador@test.com` vía API) para partir de un estado limpio. Socio: (1) wizard completo de rutina — cada paso probado en estado inválido donde el valor por defecto lo permite (paso 3 datos físicos con sexo/peso/altura vacíos bloquea con mensaje inline; paso 4 preferencias con 9 días fuera de rango 1-7 bloquea y con 4 desbloquea) antes de completarlo con datos válidos, navegación atrás en mitad del wizard (paso 3 → paso 2 → paso 3) confirmando que peso/altura/sexo ya introducidos no se pierden, envío final `POST /asistente/borrador` → 201 y aviso obligatorio "pendiente de revisión profesional" visible; (2) lo mismo para plan_nutricional (paso 4 con 1 comida fuera de rango 2-6 bloquea, 4 desbloquea), tras recargar `/asistente` (nueva instancia de `WizardBorrador`, el wizard no se resetea solo con "Atrás", hay que renavegar); (3) un tercer borrador rápido (plan_nutricional) para tener 3 borradores pendientes con los que probar las 3 acciones del entrenador por separado. Entrenador: (4) confirmado que la rutina y ambos planes nutricionales aparecen con su panel de `datos_cuestionario` correcto según tipo (equipamiento/nivel de actividad en rutina; preferencia alimentaria/comidas al día en plan) más edad y membresía resueltos (sin "—"); (5) las 3 acciones sobre 3 borradores distintos: "Aprobar" tal cual sobre un plan_nutricional (200, crea el plan con su nombre original), "Guardar cambios y aprobar" sobre el segundo plan_nutricional editando `nombre` en el textarea (PATCH 200 → aprobar 200, el plan real creado tiene el nombre EDITADO, no el original), "Rechazar" sobre la rutina con motivo obligatorio (botón deshabilitado sin motivo, 200 tras rellenarlo, no crea ninguna rutina real); (6) tras las 3 acciones no queda ninguna card pendiente de este socio (recuento antes/después consistente, baja en 3). Nota de diseño: las 2 acciones que aprueban se hicieron sobre plan_nutricional y no sobre la rutina — al reproducir el bug ya documentado en T19 (`_validar_contenido_rutina`/`DIAS_SEMANA` sin tildes rechazando "miércoles" generado por Ollama) también en esta pasada (una rutina aprobada devolvió error y quedó atascada en pendientes), se decidió, siguiendo el mismo criterio que T19, verificar ambas acciones de aprobación sobre plan_nutricional (inmune al bug, sin día de la semana) y usar la rutina para "Rechazar" (que nunca llega al validador de creación); el bug en sí sigue sin arreglarse, fuera de alcance de este módulo (backend de 007). Hallazgo adicional, backend, módulo 003 (socios), no relacionado con T21 ni corregido aquí: `SocioUpdate` (`socios/schemas.py`) declara `fecha_nacimiento: date | None`, pero la columna en BD es `NOT NULL` (`socios/models.py`) — un `PUT /socios/{id}` con `{"fecha_nacimiento": null}` devuelve 500 (Internal Server Error) en vez de una validación limpia (422); esto también confirma que el estado inválido del Paso 2 del wizard (fecha de nacimiento sin registrar) es efectivamente inalcanzable con un socio dado de alta por el flujo normal del admin (la alta exige `fecha_nacimiento` obligatoria y no hay manera limpia de vaciarla después), así que ese paso concreto no se pudo ejercitar en estado inválido en esta verificación — el resto de pasos que sí validan (tipo ya viene preseleccionado y válido; datos físicos; preferencias) se probaron en inválido y válido según lo pedido. `npm run build`/`npm run lint` limpios (mismos 3 warnings preexistentes: `panel-admin/hooks/useAuth.jsx`, `portal-socio/hooks/useAuth.jsx`, `panel-admin/pages/ClasesPage.jsx`, ninguno nuevo). Con T21 quedan las 21 tareas del módulo 016 (T1-T21) marcadas [X].
 